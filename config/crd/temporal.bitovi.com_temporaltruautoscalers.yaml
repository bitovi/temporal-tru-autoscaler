---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: temporaltruautoscalers.temporal.bitovi.com
spec:
  group: temporal.bitovi.com
  names:
    kind: TemporalTRUAutoscaler
    listKind: TemporalTRUAutoscalerList
    plural: temporaltruautoscalers
    shortNames:
    - ttautoscaler
    singular: temporaltruautoscaler
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.temporalNamespace
      name: Namespace
      type: string
    - jsonPath: .spec.minTRU
      name: MinTRU
      type: integer
    - jsonPath: .spec.maxTRU
      name: MaxTRU
      type: integer
    - jsonPath: .status.currentTRU
      name: CurrentTRU
      type: integer
    - jsonPath: .status.lastScaleTime
      name: LastScale
      type: string
    - jsonPath: .status.lastScaleDirection
      name: Direction
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          TemporalTRUAutoscaler is the Schema for the temporaltruautoscalers API.
          One resource manages the TRU level for a single Temporal Cloud namespace.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TemporalTRUAutoscalerSpec defines the desired state of TemporalTRUAutoscaler.
            properties:
              credentialsSecretRef:
                description: |-
                  CredentialsSecretRef references the Kubernetes Secret containing the
                  Temporal Cloud API key (key: "apiKey").
                properties:
                  name:
                    description: Name is the name of the Secret in the same namespace
                      as the controller.
                    type: string
                required:
                - name
                type: object
              maxTRU:
                description: MaxTRU is the maximum TRU level; the controller will
                  never scale above this.
                minimum: 1
                type: integer
              minTRU:
                description: MinTRU is the minimum TRU level; the controller will
                  never scale below this.
                minimum: 1
                type: integer
              observabilitySecretRef:
                description: |-
                  ObservabilitySecretRef optionally references a Kubernetes Secret containing
                  mTLS credentials for the Temporal Cloud metrics endpoint (keys: "clientCert"
                  and "clientKey", both PEM-encoded). Required for Temporal Cloud accounts that
                  have an observability CA certificate configured (Settings → Observability).
                  When absent, the controller authenticates to the metrics endpoint using the
                  API key (Bearer token), which works for accounts without a CA cert configured.
                properties:
                  name:
                    description: Name is the name of the Secret in the same namespace
                      as the controller.
                    type: string
                required:
                - name
                type: object
              scaleDownCooldown:
                default: 1h
                description: |-
                  ScaleDownCooldown is the minimum duration between successive scale-down actions.
                  Defaults to 1 hour because Temporal Cloud bills for the full hour after a TRU change.
                type: string
              scaleDownThreshold:
                default: 70
                description: |-
                  ScaleDownThreshold is the APS utilization percentage below which scale-down
                  is considered (must be sustained for the full scaleDownCooldown window).
                maximum: 100
                minimum: 1
                type: integer
              scaleUpCooldown:
                default: 5m
                description: |-
                  ScaleUpCooldown is the minimum duration between successive scale-up actions.
                  Defaults to 5 minutes.
                type: string
              scaleUpThreshold:
                default: 70
                description: |-
                  ScaleUpThreshold is the APS utilization percentage of the current tier ceiling
                  that triggers a scale-up action.
                maximum: 100
                minimum: 1
                type: integer
              temporalNamespace:
                description: TemporalNamespace is the Temporal Cloud namespace to
                  manage.
                minLength: 1
                type: string
            required:
            - credentialsSecretRef
            - maxTRU
            - minTRU
            - temporalNamespace
            type: object
          status:
            description: TemporalTRUAutoscalerStatus defines the observed state of
              TemporalTRUAutoscaler.
            properties:
              conditions:
                description: |-
                  Conditions contains the current conditions of the autoscaler.
                  Known condition types: Scaling, AtMinimum, AtMaximum, Ready.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentTRU:
                description: CurrentTRU is the TRU level currently reported by the
                  Temporal Cloud API.
                type: integer
              lastScaleDirection:
                description: LastScaleDirection indicates whether the most recent
                  scale action was Up or Down.
                enum:
                - Up
                - Down
                type: string
              lastScaleTime:
                description: LastScaleTime is the timestamp of the most recent scale
                  action.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
